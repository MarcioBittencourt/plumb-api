{
    "type": "postgres",
    "host": "localhost",
    "port": 5432,
    "username": "postgres",
    "password": "docker",
    "database": "plumb",
    "entities": ["dist/**/*.entity{.ts,.js}"],
    "migrations": [
        "dist/**/database/migrations/*.ts"
    ],
    "cli": {
        "migrationsDir": "dist/**/database/migrations"
    }
}